$(document).ready(function () {

   /* if ($('#dropdownHeader').click()) {
        $('.dropdown-menu-right').addClass('show');
        console.log(1)
    }
    else {
        $('.dropdown-menu-right').removeClass('show');
    }
    $(document).mouseup(function (e){ // событие клика по веб-документу
        let div = $("#dropdownHeader"); // тут указываем ID элемента
        if (!div.is(e.target) // если клик был не по нашему блоку
            && div.has(e.target).length === 0) { // и не по его дочерним элементам
            div.removeClass('show'); // скрываем его
        }
    });


    document.onclick = function() {
        document.getElementById('dropdown-menu-right').addClass('show');
    }*/




});
